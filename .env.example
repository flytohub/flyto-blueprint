# Flyto2 Blueprint does not require environment variables for the default
# in-memory or SQLite backend.

# Optional Firestore storage backend.
GOOGLE_CLOUD_PROJECT=
FIRESTORE_DATABASE=

# Optional local SQLite storage path for examples and smoke tests.
FLYTO_BLUEPRINT_DB=./blueprints.sqlite3
